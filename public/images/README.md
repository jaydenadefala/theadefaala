Drop your portrait here as `hero.jpg` (recommend a 3:4 crop, ~1200×1600px,
optimized/compressed). Until it exists, the opening scene shows a "TA"
monogram fallback so the build never breaks on a missing file.
